function isbiggerthan400(start, destination) {
    // 600 fare is =2 cents/foot  600*2
    //1500 2ct/fooot  1500*2 
    //generalize
    if (distanceInFeet <= 400) {
        return 
        //** is between <401&2000)
    //think how tell if  anumber is begger than 400 and smaller than 2000
    
    Math.max()
    return 2 * 
    
    distanceInFeet 2001 and 2500
    }




//generalize
if (distanceInFeet start <= 400) {
    return 
    //** is between <401&2000)
//think how tell if  anumber is begger than 400 and smaller than 2000

Math.max()
return 2 * 

distanceInFeet 2001 and 2500
}
